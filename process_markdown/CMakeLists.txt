CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

PROJECT(COLLATE_MARKDOWN_ARTILE)

ADD_DEFINITIONS(-std=c++11)

ADD_DEFINITIONS(-g -ggdb)

AUX_SOURCE_DIRECTORY(. DIR_SRCS)

ADD_EXECUTABLE(collateMarkdownArtile ${DIR_SRCS})

TARGET_LINK_LIBRARIES(collateMarkdownArtile sqlite3)